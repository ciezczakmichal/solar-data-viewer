{
    "name": "solar-data-viewer-package",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "calculation",
        "format",
        "ui"
    ],
    "scripts": {
        "calculation": "yarn workspace calculation",
        "format": "yarn workspace format",
        "ui": "yarn workspace ui",
        "build": "yarn build:core && yarn build:ui",
        "build:core": "yarn build:format && yarn build:calculation",
        "build:calculation": "yarn calculation build",
        "build:format": "yarn format build",
        "build:ui": "yarn ui build",
        "dev": "yarn build:core && yarn ui dev",
        "start": "yarn ui start",
        "test": "yarn build:core && yarn calculation test && yarn ui test",
        "check": "yarn ui check"
    }
}
