// wymagane przez class-transformer
import 'reflect-metadata'

// import { convertObjectToDataFormat } from './converter'

describe('convertObjectToDataFormat', () => {
    it('test, który niczego nie testuje :(', () => {})

    // @todo jest problem z uruchomieniem testów - błędy Jest
    // "SyntaxError: The requested module X does not provide an export named Y"
    // it('null i undefined nie jest poprawnym formatem danych', async () => {
    // await expect(() =>
    //     convertObjectToDataFormat(null)
    // ).rejects.toThrowError()
    // })
})
